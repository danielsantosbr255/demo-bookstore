services:
  backend-prod:
    build:
      context: ./backend
      target: prod
    image: demo-bookstore:prod
    env_file:
      - .env
    ports:
      - 8081:8080
    networks:
      - network

networks:
  network:
